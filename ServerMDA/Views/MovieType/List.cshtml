@model IEnumerable<ServerMDA.Models.電影片種movieType>

@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.片種編號type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.電影編號movie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.片種編號type.次片種名稱typeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.電影編號movie.英文標題titleEng)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.電影片種編號mtId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.電影片種編號mtId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.電影片種編號mtId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
