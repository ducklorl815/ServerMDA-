@model IEnumerable<ServerMDA.Models.票種ticketType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.票種名稱ticketTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.票種名稱ticketTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.票種編號ticketTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.票種編號ticketTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.票種編號ticketTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
