@using ServerMDA.ViewModel
@model CMovieImagesListViewModel

@{
    ViewData["Title"] = "新增";
}


<h4 class="offset-md-1">新增電影清單</h4>
<div class="row">
    <div class="col-md-4 offset-md-1">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="電影編號movieId" class="control-label"></label>
                <select asp-for="電影編號movieId" name="中文標題titleCht" class="form-control" asp-items="ViewBag.電影編號movieId"></select>
            </div>
            <div class="form-group">
                <label asp-for="圖片編號imageId" class="control-label"></label>
                <select asp-for="圖片編號imageId" class="form-control" asp-items="ViewBag.圖片編號imageId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="新增" class="btn btn-primary mr-2 pr-4 offset-md-10" />
                <a asp-action="List" class="btn btn-secondary">回到清單</a>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
